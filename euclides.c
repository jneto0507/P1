#include <stdio.h>

int euc(int a, int b, int r)
{
    if (a % b == 0)
    {
        return b;
    }
    else (a % b != 0)
    {
        a % b == r;
        b / r == b;
        euc(int a, int b)
    }
}

int loop()
{

}

int main()
{
    int a, b, r, b1;
    scanf("%d %d", &a, &b);
    b1 = b;
    euc(a, b, r);
    printf("d\n", &b);
    return 0;
}
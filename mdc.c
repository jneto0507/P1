#include <stdio.h>

int mdc(int a, int b)
{
    int n;
    if(a % n == 0 && b % n == 0)
    {
        return n;
    }
    else
    {
        
    }
    
}

int main()
{
    int a, b, r, n, res;
    scanf("%d %d", &a, &b);
    if(a > b)
    {
        a = res;
        b = a;
        res = b;
        mdc(a, b);
    }
       
}